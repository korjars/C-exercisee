#include<stdio.h>
#include<string.h>
#include<strings.h>
#include<stdbool.h>

#define Maxlen 255

typedef struct 
{
    char ch[Maxlen];
    int length;
}SString;

typedef struct 
{
    char *ch;
    int length;
}HString;


int next[];
int nextval[];

