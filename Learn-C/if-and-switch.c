#include<stdio.h>
int main(void)
{
    if (0)
    {printf("测试if功能\n");
    printf("test the founction of if\n");
    printf("test the useage of if founction\n");}
//if 只能保证下一句，除非括号
//if 只能保证下一句，除非括号
//if 只能保证下一句，除非括号
//if 只能保证下一句，除非括号
//if 只能保证下一句，除非括号
//if 只能保证下一句，除非括号
//if 只能保证下一句，除非括号
//if 只能保证下一句，除非括号
//if 只能保证下一句，除非括号
//if 只能保证下一句，除非括号
//if 只能保证下一句，除非括号
//if 只能保证下一句，除非括号
    int a;
    scanf("%d",&a);
    switch (a)
    {
    case 1:printf("1");printf("1");printf("1");printf("1");printf("1\n");break;
    case 2:printf("1");printf("1");printf("1");printf("1");printf("1\n");//break;
    case 3:printf("1");printf("1");printf("1");printf("1");printf("1\n");//break;
    case 4:printf("1");printf("1");printf("1");printf("1");printf("1\n");//break;
    case 5:printf("1");printf("1");printf("1");printf("1");printf("1\n");//break;
    case 6:printf("1");printf("1");printf("1");printf("1");printf("1\n");//break;
    case 7:printf("1");printf("1");printf("1");printf("1");printf("1\n");//break;
    default:
        break;
    }
//switch 没有break下面的全部会执行全部会执行
//switch 没有break下面的全部会执行全部会执行
//switch 没有break下面的全部会执行全部会执行
//switch 没有break下面的全部会执行全部会执行
//switch 没有break下面的全部会执行全部会执行
//switch 没有break下面的全部会执行全部会执行
//switch 没有break下面的全部会执行全部会执行
//switch 没有break下面的全部会执行全部会执行
//switch 没有break下面的全部会执行全部会执行
//switch 没有break下面的全部会执行全部会执行
//switch 没有break下面的全部会执行全部会执行
//switch 没有break下面的全部会执行全部会执行
//switch 没有break下面的全部会执行全部会执行
//switch 没有break下面的全部会执行全部会执行
//switch 没有break下面的全部会执行全部会执行
    return 0;

}